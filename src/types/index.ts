export interface MapPinCard {
  title: string;
  description: string;
  date: string;
  rotate?: number;
  delay?: number;
}

export interface reviewCard {
  name: string;
  review: string;
  classes: number;
}